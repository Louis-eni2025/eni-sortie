{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-sm-6 mx-auto">
        <h1 class="h1Page">Créer une ville</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Valider</button>
        {{ form_end(form) }}
    </div>

{% endblock %}

{% block title %}
    Créer une ville
{% endblock %}

