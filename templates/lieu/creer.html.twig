{% extends 'base.html.twig' %}

{% block body %}
    <h1>Créer un lieu</h1>
    {{ form_start(lieuForm) }}
    {{ form_widget(lieuForm) }}
    <button type="submit" class="btn btn-primary">Valider</button>
    {{ form_end(lieuForm) }}
{% endblock %}

{% block title %}
Créer un lieu
{% endblock %}

