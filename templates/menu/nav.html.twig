<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <img id="logoBucket" src="{{ asset('img/eni_800x800.jpg') }}" alt="logo" width="40px" class="me-3">
        <a class="navbar-brand" href="#">ENI - Sortie</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('main_accueil') }}">Home</a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link active"
                           href="{{ path('app_logout') }}">Logout {{ app.user.userIdentifier }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active"
                           href="{{ path('utilisateur_profil', {id: app.user.id}) }}">Profil</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>